<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.b2b2c.dao.paymentway.PaymentWayMapper">
	<select id="getAll" resultType="PaymentWay">
		SELECT pw.* FROM `payment_way` pw
	</select>
	
	<select id="getId" resultType="PaymentWay">
		SELECT pw.* FROM `payment_way` pw WHERE `id`=#{pId}
	</select>
	
	
	
	
	
</mapper>